import { createTheme, ThemeProvider, styled } from "@mui/material/styles"

export {my_theme}

const my_theme = createTheme({
    palette: {
        mode: "dark",
        divider: "#ffffff77"
      },
})
